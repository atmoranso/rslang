import ElementTemplate from '../../../common/ElementTemplate';

export default class StatisticView extends ElementTemplate {
  constructor(parentNode: HTMLElement | null) {
    super(parentNode, 'div', '', 'Статистика');
  }
}
